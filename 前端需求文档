# 通用英语对话练习 · 网页 Demo 前端功能需求

> **文档版本**：v0.1.6  
> **更新时间**：2026-05-09  
> **适用范围**：本文档描述 **C 端网页 Demo** 的页面、导航、控件状态与验收口径；与 **`prd-scenario-package-v2.md`**（下称「场景包 PRD」，与本文件同目录）在字段语义上对齐，并在 **章节导航** 上采用 **§5.6.1 Demo 豁免**（见该 PRD）。工程落地参见同目录 **`technical-implementation-skeleton.md`**。  
> **非目标**：账号体系、支付、安装态引导、生产级权限与埋点规范。

---

## 0. 与场景包 PRD 的关系

| 主题 | 场景包 PRD | 本文档（Demo） |
|------|------------|----------------|
| 场景包隔离、八类智能体字段语义 | §5、§6 | 页面与交互须能支撑该语义；不在此重复字段表 |
| 小节资产分区（§5.7） | 必须 | P2.4 取消、P2.1 重置创作时的清除范围与 PRD 一致 |
| 线性推进（§5.6） | **完整产品**默认硬规则 | **豁免**：见场景包 PRD **§5.6.1**；P3a 允许同包内任意小节切换 |
| R1 回答提示（§6.7） | 仅等待用户回复时 | P3「回答提示」弹窗遵守相同触发与展示规则 |
| R2 本节复盘（§6.8、§7） | 显式点击、成功覆盖/失败保留 | P3「总结分析」弹窗遵守 |
| `turn_writer`、NPC–NPC 节奏（§6.6） | 必须 | P3 发送消息仅影响用户回合的 `recipient_id`；其余由后端/状态机实现 |

**术语**：界面「语言场景」列表中的每一项 = **一个场景包**（`scenario_id`）。

---

## 1. 页面总览

| 编号 | 页面名称 | 主要作用 | 核心布局要点 |
|------|----------|----------|----------------|
| P1 | 欢迎页 | 演示入口心智；每次进入产品首屏 | 标题、简介、主视觉占位、「知道了」、右上「调试模式」 |
| P2 | 语言场景清单 | 管理多个场景包：进入或删除；创建新包 | 场景包卡片网格/列表、「创建新场景」主按钮、右上「调试模式」 |
| P2a | 场景包操作浮层 | 对所选包执行删除或进入 | 模态/底部动作表：「删除场景」「进入场景」 |
| P2b | 删除确认 | 防止误删 | 二次确认文案、「确认」「取消」 |
| P2.1 | 基本描述（向导步 1） | 采集用户五字段；可触发「重置创作」 | 顶栏三 Tab（**仅进度指示，禁止点击跳步**）、表单、「返回」「下一步」、右上「调试模式」 |
| P2.2 | 框架渲染中 | 等待 §6.2（及编排上的关联步骤）完成 | 全屏 Loading、**当前进度文案**（见 **G10**）、「取消」 |
| P2.3 | 框架预览（向导步 2） | 预览剧情框架 + 参与人物 | 框架树、人物清单、「返回」「下一步」、三 Tab |
| P2.4 | 专属世界渲染中 | 等待全书小节扩写与任务（§6.4/§6.5）完成 | 全屏 Loading、**当前进度文案**（见 **G10**）、「取消」 |
| P2.5 | 生成世界（向导步 3） | 按小节预览内容提要、任务、出场人物；结束创作 | 可展开小节列表、两主按钮；**无「上一步」**；三 Tab |
| P3 | 对话练习 | 运行期主界面：聊天 + 顶栏工具 | 顶栏（返回首页、背景介绍、回答提示、总结分析、查看列表、调试模式）、**本节全量聊天记录**、气泡 + typing、底区「选择信息接收人」（多 NPC 时为浮层选人）、输入框、「发送」 |
| P3a | 章节列表弹窗 | **同包内任意切换**当前练习小节（Demo 豁免） | 全屏或近全屏模态：章—节树、每节摘要/任务/人物、「关闭」「确认进入」 |
| DBG | 调试独立页 | 与 C 端无关的全链路调试 | 由「调试模式」进入；须能返回 C 端入口栈 |

---

## 2. 全局规则（跨页面）

### G1 调试模式

- **入口**：P1、P2、P2.1、P2.3、P2.5、P3 等 C 端页面右上角「调试模式」常驻（Demo 不设权限开关）。  
- **行为**：点击后导航至 **独立调试页 DBG**；不嵌入当前 C 端 Tab 壳层。  
- **返回**：由调试页提供返回上一 C 端或首页的策略（技术实现任选，需在实现层一致）。

### G2 创作向导顶栏三 Tab

- 文案顺序固定：**基本描述** → **框架预览** → **生成世界**。  
- **禁止**通过点击 Tab 跳转到未完成步骤；点击时 **无导航** 或 **Toast**：「请先完成当前步骤」。  
- Tab 高亮仅反映当前真实所在步骤。

### G3「重置创作」硬规则（与场景包 PRD 一致）

当某场景包已存在 **`story_framework`（或等价「框架已生成」标志）为真** 时，若用户在 **P2.1** 对**任意**表单字段发生编辑并再次点击 **「下一步」** 进入 P2.2：

- **必须**删除该包下的：`story_framework`、角色表（§6.3 产出）、以及**尚未进入运行期**的全部 §6.4 / §6.5 分区内容（若曾生成）。  
- **不得**影响其他 `scenario_id` 的场景包。  
- UI 可选：首次检测到「将重置」时弹出二次确认（产品可选）；若不做确认，也须在需求上接受「静默覆盖」。

### G4 P2.4「取消」清除范围

用户于 P2.4 点击「取消」回到 P2.3 时：

- **必须**清除该包下**全部**已落库的 `section_narrative` 与 `section_mission`（及与本次世界渲染任务绑定的中间态）。  
- **保留** P2.3 已展示的框架与人物清单（除非用户随后走 G3 从 P2.1 再次重置）。

### G5 P2.5 之后入口约束

- 用户进入 **P2.5** 后，**不提供**返回 P2.1 的向导路径（无「上一步」）。  
- **当前 Demo 不提供**从运行期（P3）回到该包 P2.1 的入口；若未来增加「编辑场景信息」，须走「新建场景包」或在独立需求中明确「触发 G3 重置创作」。

### G6 P3「回答提示」（R1）

- **仅当**系统状态为「等待用户回复」且已存在可对齐的 `target_turn_id`（语义见场景包 PRD §6.7）时：允许打开弹窗并请求/展示建议。  
- **否则**：按钮 **置灰** 或点击后 Toast：「当前无需回复建议」。  
- 弹窗可关闭；关闭不改变对话状态。

### G7 P3「总结分析」（R2）

- 用户每次点击：触发一次 §6.8 对应流程；Loading 文案固定为：**「正在生成中，请稍后」**。  
- **成功**：新结果 **覆盖** 同小节已存在的成功分析内容（展示层与持久层策略一致）。  
- **失败**：展示可读错误；**保留**上一份已成功内容（若有）。  
- **禁止**后台静默定时自动生成。

### G8 P3 底部「选择信息接收人」

- **仅约束**用户**即将发送**的一条消息的 `recipient_id`。  
- **不**约束模型生成 NPC 台词时的说话对象编排（由对话生成与 §6.6.4 等规则负责）。  
- **候选范围**：仅限**当前小节** `section_narrative.appearing_npc_ids` 中的 NPC（不含 `user`）；顺序与角色表展示顺序由技术约定且须**稳定**（多 NPC 时「默认第一个」指该稳定顺序之首项）。  
- **仅 1 名 NPC 在本节出场**：进入该小节或切换到该节后，**默认选中**该 NPC；发送时 `recipient_id` **必须**为该 NPC。入口可展示为「当前接收人：{姓名}」；点击「选择信息接收人」时，可展示**仅含一项**的浮层，或省略浮层（实现二选一，须在整应用内一致）。  
- **多名 NPC 在本节出场**：进入或切换到该节后，**默认选中**上述稳定顺序中的**第一位** NPC；用户点击「选择信息接收人」须弹出**浮层**（底部动作表或居中模态均可），列出本节全部出场 NPC 供点选；用户选定后浮层关闭，底部回显当前选中 NPC；点击「发送」时 `recipient_id` **必须**为用户当前选中的 NPC。

### G9 P3a 与 §5.6.1（Demo 豁免）

- 用户在 **P3a** 中可选择 **任意**合法 `(chapter_id, section_id)` 并「确认进入」。  
- 确认后：更新运行指针为该节，P3 聊天区切换为**该节**上下文（历史回合按该节加载；见技术方案）。  
- **不适用**场景包 PRD §5.6 中「禁止跨节跳跃 / 禁止回退写」的**完整产品**约束；完整产品若收敛为严格线性，应移除本豁免并改交互。

### G10 长耗时创作页的「分步进度」展示（P2.2、P2.4）

创作流水线单次等待可能较长，须在 **P2.2、P2.4** 主区提供 **「当前正在生成什么」** 的显性反馈，降低死等体感。

**布局要素（两页通用）**

1. **主标题行**（固定或弱变化）：P2.2 如「场景框架渲染中」；P2.4 如「专属世界渲染中」。  
2. **当前进度文案**（**必填展示位**）：单行或多行短文案，概括**此刻正在执行的子步骤**；推荐句式前缀 **「正在生成…」** / **「正在处理…」**，内容与后端/编排下发的阶段信息一致（见下）。  
3. **Loading 动效**（既有）：与进度文案同时存在。  
4. **「取消」**（既有）：行为不变。

**进度文案的数据来源（产品约束）**

- **优先**：由接口轮询、WebSocket / SSE 或任务状态中的 **`current_step_label`（或语义等价字段）** 驱动；文案随阶段变化**实时更新**（例如 P2.2：「正在生成剧情框架…」→「正在生成参与角色…」；P2.4：「正在生成第 1 章第 1 节场景…」→「正在为第 1 章第 1 节生成任务目标…」→下一小节…）。具体阶段枚举由**技术方案**与后端编排对齐本文档的**展示意图**即可。  
- **降级**：若短期内无阶段字段，前端**必须**仍显示静态兜底句（P2.2：**「正在生成场景框架，请稍候」**；P2.4：**「正在生成各小节内容，请稍候」**）；不得出现无说明的纯白屏等待。  
- **诚实原则**：不得用**虚假**进度百分比欺骗用户；若仅能提供粗粒度阶段，只用文案切换即可。

---

## 3. 分页面详细需求

以下结构统一为：

- **页面作用与布局**  
- **功能清单**（每个功能含 **全量用户故事**：前置条件、操作、预期结果）

---

### 3.1 P1 欢迎页

#### 页面作用与布局

- **作用**：品牌与价值一句话传达；作为 **每次进入产品的首屏**（演示策略）。  
- **布局**：顶栏含「调试模式」；主区标题「欢迎来到自由语言世界」、简介段落、主视觉占位；底栏主按钮「知道了」。

#### 功能清单

##### F-P1-01 展示欢迎内容

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P1-01-01 | 任意 | 进入产品首屏 | 看到标题、简介、主视觉区域；无控制台报错（验收以人工/自动化约定为准） |
| P1-01-02 | 任意 | 阅读简介 | 文案只读展示，不可编辑 |
| P1-01-03 | 弱网模拟 | 进入首屏 | 布局不错位；若资源失败有占位或重试（可选，Demo 可简化） |

##### F-P1-02 进入场景清单

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P1-02-01 | 在 P1 | 点击「知道了」 | 导航至 **P2** |
| P1-02-02 | 在 P1 | 连点「知道了」 | 仅一次有效跳转，无重复路由栈异常（实现层保证） |

##### F-P1-03 调试模式

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P1-03-01 | 在 P1 | 点击「调试模式」 | 打开 **DBG** 调试独立页 |
| P1-03-02 | 在 P1 | 从 DBG 返回 | 回到 P1 或约定入口（与 G1 一致） |

---

### 3.2 P2 语言场景清单

#### 页面作用与布局

- **作用**：列出用户已有场景包；进入运行或删除；发起新建。  
- **布局**：顶栏「调试模式」；主区为场景包入口列表（每项展示包标题等）；底部主按钮「创建新场景」。

#### 功能清单

##### F-P2-01 展示场景包列表

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2-01-01 | 本地无场景包 | 进入 P2 | 见空列表态与引导（可为空态插画+「创建新场景」） |
| P2-01-02 | 已有 ≥1 个包 | 进入 P2 | 每个包至少展示标题；顺序稳定（如更新时间倒序，由技术约定） |
| P2-01-03 | 已有包 | 滚动列表 | 列表可滚动；底部主按钮常驻或易达 |

##### F-P2-02 打开场景包操作浮层（P2a）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2-02-01 | 在 P2 | 点击某一场景包卡片/按钮 | 弹出 **P2a** |
| P2-02-02 | P2a 已打开 | 点击浮层外交互区（遮罩） | 关闭 P2a，无状态变更 |
| P2-02-03 | P2a 已打开 | 按 Esc（若 Web 实现） | 关闭 P2a（可选） |

##### F-P2-03 创建新场景

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2-03-01 | 在 P2 | 点击「创建新场景」 | 进入 **P2.1**，且目标为新包或新建草稿流（由技术绑定 `scenario_id` 创建时机） |
| P2-03-02 | 在 P2 | 弱网点击创建 | 显示加载或禁用防重复提交；失败 Toast（可选） |

##### F-P2-04 调试模式

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2-04-01 | 在 P2 | 点击「调试模式」 | 进入 **DBG** |

---

### 3.3 P2a 场景包操作浮层

#### 页面作用与布局

- **作用**：对指定包执行「删除」或「进入」。  
- **布局**：模态；按钮「删除场景」（危险态样式）、「进入场景」（主按钮样式）。

#### 功能清单

##### F-P2a-01 删除场景

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2a-01-01 | P2a 打开 | 点击「删除场景」 | 弹出 **P2b** |
| P2a-01-02 | P2b 打开 | 点击「取消」 | 关闭 P2b，仍停留在 P2a 或返回 P2（实现二选一须一致；建议关 P2b 留 P2a） |
| P2a-01-03 | P2b 确认删除成功 | — | 该包从列表移除，关闭浮层，回到 P2 |

##### F-P2a-02 进入场景

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2a-02-01 | 所选包已完成创作（`creation_succeeded` 或已进入运行期，语义由技术映射） | 点击「进入场景」 | 关闭浮层，进入 **P3**；加载该包运行态 |
| P2a-02-02 | 所选包创作未完成 | 点击「进入场景」 | **禁止**进入 P3；Toast 或禁用按钮说明「请先完成场景创建」（文案可产品定稿） |
| P2a-02-03 | 在 P2a | 点击「进入场景」且后端失败 | 错误提示；留在 P2a 或 P2 |

---

### 3.4 P2b 删除确认

#### 页面作用与布局

- **作用**：删除场景包及数据的二次确认。  
- **布局**：文案「是否确认删除该场景及数据」；按钮「确认」「取消」。

#### 功能清单

##### F-P2b-01 确认删除

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2b-01-01 | P2b 打开 | 点击「确认」 | 执行删除；成功后关闭链路至 P2；列表不再展示该包 |
| P2b-01-02 | P2b 打开 | 点击「确认」但接口失败 | 展示错误；数据仍在；可重试 |
| P2b-01-03 | P2b 打开 | 点击「取消」 | 关闭 P2b，不删除 |

---

### 3.5 P2.1 基本描述（向导步 1）

#### 页面作用与布局

- **作用**：录入与场景包 PRD §6.1 对齐的五类用户输入（展示名可与 PRD 字段映射）。  
- **推荐字段标签**：场景名称；你的身份；你使用的场景；你的目标；学伴偏好（第五项为可选偏好/词表类输入，技术映射 `vocabulary_list` 或等价字段）。  
- **布局**：三 Tab（只读进度）；表单区；底栏「返回」（至 P2）、「下一步」（至 P2.2）；右上「调试模式」。

#### 功能清单

##### F-P2.1-01 表单校验与提交

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.1-01-01 | 必填项未填完 | 点击「下一步」 | 阻止提交；字段级或统一错误提示 |
| P2.1-01-02 | 文本长度低于 PRD 下限（如场景描述过短，若前端预校验） | 点击「下一步」 | 阻止并提示（与 §6.1 一致时由产品给出具体阈值文案） |
| P2.1-01-03 | 所有校验通过；包内**尚无** `story_framework` | 点击「下一步」 | 进入 **P2.2**；发起框架生成任务 |
| P2.1-01-04 | 所有校验通过；包内**已有** `story_framework`（从 P2.3 返回修改） | 点击「下一步」 | 触发 **G3 重置创作** 后进入 **P2.2** |

##### F-P2.1-02 返回列表

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.1-02-01 | 在 P2.1 | 点击「返回」 | 回到 **P2**；已填草稿是否保留由技术定（建议保留草稿以免丢字） |

##### F-P2.1-03 Tab 防跳步（G2）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.1-03-01 | 在 P2.1 | 点击「框架预览」或「生成世界」Tab | 无跳转或 Toast「请先完成当前步骤」 |

##### F-P2.1-04 调试模式

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.1-04-01 | 在 P2.1 | 点击「调试模式」 | 进入 **DBG** |

---

### 3.6 P2.2 框架渲染中

#### 页面作用与布局

- **作用**：阻塞等待框架（及编排上的下一步依赖，如剧情框架、角色清单等）生成。  
- **布局**：主标题区（如「场景框架渲染中」）；**当前进度文案区**（**G10**）；Loading 动效；底栏「取消」。可选：副标题展示当前场景包标题（`scenario_title`），便于用户确认「在为谁生成」。

#### 功能清单

##### F-P2.2-00 分步进度展示（G10）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.2-00-01 | 刚进入 P2.2 | 注视页面 | 除 Loading 外，**立刻**可见主标题 + **当前进度文案**（接口已有则显示服务端文案，否则显示 **G10 降级** 兜底句） |
| P2.2-00-02 | 后端在任务过程中推送/轮询到新的阶段 | 无操作 | **当前进度文案**更新为新阶段描述，用户能感知「在推进」 |
| P2.2-00-03 | 全程仅有粗粒度一种阶段 | 无操作 | 进度文案可不变或与主标题合并展示，但**不得**整段等待无任何文字说明（须满足兜底） |
| P2.2-00-04 | 接口异常拿不到阶段但任务仍在跑 | 无操作 | 保留兜底句 + 可选 Toast「进度信息暂不可用」（可选）；Loading 仍显示 |

##### F-P2.2-01 取消任务（G2 与清除策略）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.2-01-01 | 在 P2.2；任务进行中 | 点击「取消」 | 中断本次请求；回到 **P2.1**；丢弃本次任务产生的**中间态**（无稳定框架落库或回滚至进入 P2.2 前状态，由技术实现保证与产品「丢弃本次任务」一致） |
| P2.2-01-02 | 在 P2.2；任务已成功结束瞬间 | 点击「取消」 | 取消无效或按钮已禁用；随后应自动进入 **P2.3**（由轮询/推送完成触发） |

##### F-P2.2-02 成功完成

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.2-02-01 | 任务成功 | 无操作 | 自动进入 **P2.3** 并展示框架与人物 |

---

### 3.7 P2.3 框架预览（向导步 2）

#### 页面作用与布局

- **作用**：展示 `story_framework` 与角色清单供用户确认。  
- **布局**：剧情框架树；参与人物清单（姓名、岗位/身份、性格等）；「返回」至 P2.1、「下一步」至 P2.4；三 Tab 中高亮「框架预览」；右上「调试模式」。

#### 功能清单

##### F-P2.3-01 返回修改

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.3-01-01 | 在 P2.3 | 点击「返回」 | 回到 **P2.1**；用户可编辑字段 |
| P2.3-01-02 | 从 P2.3 回到 P2.1 并修改任意字段 | 再次「下一步」 | 同 **G3**：清空框架、角色及未运行期的 §6.4/§6.5 |

##### F-P2.3-02 进入世界渲染

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.3-02-01 | 在 P2.3 | 点击「下一步」 | 进入 **P2.4**；发起全书小节渲染任务（编排由技术定义） |

##### F-P2.3-03 Tab 防跳步

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.3-03-01 | 在 P2.3 | 点击「基本描述」Tab | **不允许**回到 P2.1（防跳步）；无导航或 Toast（与 G2 一致） |
| P2.3-03-02 | 在 P2.3 | 点击「生成世界」Tab | 无导航或 Toast |

##### F-P2.3-04 调试模式

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.3-04-01 | 在 P2.3 | 点击「调试模式」 | 进入 **DBG** |

---

### 3.8 P2.4 专属世界渲染中

#### 页面作用与布局

- **作用**：等待所有小节 `section_narrative` / `section_mission` 按 §5.7 分区逐节生成完成（总耗时可能较长）。  
- **布局**：主标题区（如「专属世界渲染中」）；**当前进度文案区**（**G10**），宜体现**当前处理到哪一章哪一节**或「叙事 / 任务」子步骤；Loading 动效；「取消」。可选：展示 `scenario_title`。

#### 功能清单

##### F-P2.4-00 分步进度展示（G10）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.4-00-01 | 刚进入 P2.4 | 注视页面 | **立刻**可见主标题 + **当前进度文案**（有阶段信息则显示，否则 **G10 降级**「正在生成各小节内容，请稍候」） |
| P2.4-00-02 | 后端按小节或子步骤推进 | 无操作 | 进度文案随当前小节或子任务变化而更新（例如从「第 1 章第 1 节」到「第 1 章第 2 节」，或叙事与任务两句交替，由技术映射） |
| P2.4-00-03 | 单节内先叙事后任务的两段式编排 | 无操作 | 用户至少能看到该节内**阶段切换**（不必假进度条） |
| P2.4-00-04 | 阶段接口不可用 | 无操作 | 兜底句持续展示 + Loading；不出现无文案长时间静止 |

##### F-P2.4-01 取消（G4）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.4-01-01 | 在 P2.4 | 点击「取消」 | 中断任务；回到 **P2.3**；**清除该包全部** `section_narrative` 与 `section_mission` |
| P2.4-01-02 | 取消过程中接口失败 | — | 明确错误态；用户可重试取消或留在当前页（技术定） |

##### F-P2.4-02 成功完成

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.4-02-01 | 任务成功 | 无操作 | 自动进入 **P2.5** |

---

### 3.9 P2.5 生成世界（向导步 3）

#### 页面作用与布局

- **作用**：按小节展示「内容提要 / 小节任务 / 出场人物」总览；结束创作或直接进入练习。  
- **布局**：小节可展开项（单包内多节）；按钮「完成并返回列表」「直接探索世界」；**无返回上一步**；三 Tab 高亮「生成世界」；右上「调试模式」。

#### 功能清单

##### F-P2.5-01 小节预览展示

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.5-01-01 | 在 P2.5 | 展开某一节 | 展示该节内容提要、任务、出场人物 |
| P2.5-01-02 | 在 P2.5 | 折叠某一节 | 收起详情，列表简洁 |

##### F-P2.5-02 完成并返回列表

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.5-02-01 | 在 P2.5 | 点击「完成并返回列表」 | 包标记为可进入运行（`creation_succeeded` 等语义）；导航 **P2** |
| P2.5-02-02 | 在 P2.5 | 点击后保存失败 | 错误提示；留在 P2.5 |

##### F-P2.5-03 直接探索世界

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.5-03-01 | 在 P2.5 | 点击「直接探索世界」 | 进入 **P3**；默认进入 **全书第 1 小节 `S[0]`**（与场景包 PRD首次运行指针一致） |
| P2.5-03-02 | 在 P2.5 | 点击后运行态初始化失败 | 错误提示；不进入 P3 或进入空态（技术定一种） |

##### F-P2.5-04 Tab 防跳步与防回 P2.1

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.5-04-01 | 在 P2.5 | 点击「基本描述」「框架预览」Tab | 无导航或 Toast（不得回到 P2.1） |

##### F-P2.5-05 调试模式

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P2.5-05-01 | 在 P2.5 | 点击「调试模式」 | 进入 **DBG** |

---

### 3.10 P3 对话练习

#### 页面作用与布局

- **作用**：运行期主界面；展示当前小节对话；提供背景/提示/复盘/章节列表入口。  
- **布局**：  
  - **顶栏**：「返回首页」「背景介绍」「回答提示」「总结分析」「查看列表」「调试模式」。  
  - **中区**：**本节聊天记录区**——展示**当前 `(chapter_id, section_id)` 下全部已持久化回合**（见 **F-P3-07**）；聊天气泡（用户与 NPC 区分样式）、NPC 输入中指示（typing）。  
  - **底区**：「选择信息接收人」入口（见 **G8**；多 NPC 时点击打开选人浮层）、多行文本输入、「发送」。

#### 功能清单

##### F-P3-01 返回首页

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-01-01 | 在 P3 | 点击「返回首页」 | 导航至 **P2**；运行状态保留于服务端（Demo 不要求显式「暂停」文案） |
| P3-01-02 | 在 P3；输入框内有**未发送**草稿（任意非空字符） | 点击「返回首页」 | **丢弃**输入框草稿（不保留至下次进入）；导航至 **P2**；服务端已持久化回合不受影响 |

##### F-P3-02 背景介绍（弹窗）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-02-01 | 在 P3 | 点击「背景介绍」 | 打开弹窗：展示**当前小节**的叙事摘要、`section_objective`、出场人物 |
| P3-02-02 | 弹窗打开 | 点击关闭或遮罩 | 弹窗关闭；聊天区不变 |
| P3-02-03 | 当前节数据未就绪 | 打开弹窗 | 展示加载或错误说明 |

##### F-P3-03 回答提示（弹窗，R1，G6）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-03-01 | 满足 R1 条件（G6） | 点击「回答提示」 | 打开弹窗；展示与 `target_turn_id` 绑定的建议内容（结构见 §6.7） |
| P3-03-02 | 不满足 R1 | 点击「回答提示」 | 按钮置灰或 Toast「当前无需回复建议」；**不**打开空弹窗 |
| P3-03-03 | 弹窗打开且建议已 stale | 用户已发送新回复导致建议过期 | 弹窗内展示「已过期」或自动刷新为 stale 状态（与 §6.7 `hint_status` 一致） |
| P3-03-04 | 弹窗打开 | 关闭 | 关闭弹窗 |

##### F-P3-04 总结分析（弹窗，R2，G7）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-04-01 | 在 P3 | 点击「总结分析」 | 弹窗打开并显示 Loading：「正在生成中，请稍后」 |
| P3-04-02 | 生成成功 | 等待结束 | 展示新复盘正文；**覆盖**上一份成功内容 |
| P3-04-03 | 生成失败 | 等待结束 | 展示错误；**保留**上一份成功内容 |
| P3-04-04 | 本节尚无回合或不足 §6.8 输入下限 | 点击「总结分析」 | 阻止或提示「本节暂无可分析对话」（与 §6.8 `prior_turns` 下限对齐） |

##### F-P3-05 查看列表 → P3a

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-05-01 | 在 P3 | 点击「查看列表」 | 打开 **P3a** 弹窗 |
| P3-05-02 | P3a 打开 | 关闭 P3a | 不改变当前节，除非用户已在 P3a 点「确认进入」 |

##### F-P3-06 选择接收人与发送

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-06-01 | 在 P3；系统等待用户发言；已按 **G8** 有默认 `recipient_id` | 输入英文 + 点击「发送」 | 写入一条 `speaker_id=user`、`turn_writer=human_user` 的回合；`recipient_id` 为**当前选中** NPC |
| P3-06-02 | 系统未等待用户 | 尝试发送 | 发送禁用或 Toast |
| P3-06-03 | 数据异常导致无合法选中 NPC（不应常态出现） | 点击「发送」 | 阻止并 Toast；不发起写入 |
| P3-06-04 | 当前小节 **仅 1** 名出场 NPC | 进入本节或从 P3a 切换到本节 | **默认选中**该 NPC；发送时 `recipient_id` 为该 NPC |
| P3-06-05 | 当前小节 **≥2** 名出场 NPC | 进入本节或从 P3a 切换到本节 | **默认选中** `appearing_npc_ids` 稳定顺序中的**第一位** NPC |
| P3-06-06 | 当前小节 **≥2** 名出场 NPC | 点击「选择信息接收人」 | 打开**浮层**，列出本节全部出场 NPC（展示名 + 可区分信息）；当前选中项高亮 |
| P3-06-07 | 选人浮层已打开 | 点击某一 NPC | 浮层关闭；底部回显「将发给该 NPC」；后续「发送」使用该 `recipient_id` |
| P3-06-08 | 选人浮层已打开 | 点击遮罩或「取消」类操作（若有） | 关闭浮层；**不改变**发送前已选中的 NPC（仍为打开浮层前的选中项） |

##### F-P3-07 聊天记录展示、聊天气泡与 typing

**聊天记录（本节全量）**

- **数据源**：当前运行指针 `(chapter_id, section_id)` 下，服务端返回的该节全部 `turn`（与场景包 PRD §6.6.3 一致），按 **`created_at` / 服务端约定的时间升序** 排列。  
- **进入 P3 / 从 P2.5「直接探索」进入**：加载并展示该节已有全部回合；无历史时展示空态（引导用户开口或等待 NPC）。  
- **从 P3a「确认进入」切换小节**：清空主区列表后重新拉取**新小节**的全部回合并展示；输入区草稿按 **P3-01-02** 同级策略：**切换小节时丢弃未发送草稿**（须写入本文档或本表故事，见下）。  
- **用户发送成功**：本地或刷新后**立即**将新回合追加进聊天记录（或整表刷新），用户能在列表中看到**自己刚发**的一条，且顺序正确。  
- **NPC 回复写入成功**：同上，新气泡出现在记录末尾（或正确序位）。  
- **范围**：主聊天区**仅展示当前小节**记录，不混入其他小节气泡（除非技术显式做「折叠历史」——本 Demo **不要求**）。

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-07-01 | 已进入某小节且该节已有历史回合 | 打开 P3 注视列表 | 按时间升序展示**该节全部**已有用户与 NPC 消息 |
| P3-07-02 | 该节尚无回合 | 打开 P3 | 展示空态；无报错 |
| P3-07-03 | 在 P3；系统等待用户；已输入未发送草稿 | 从 P3a 确认切换到**另一小节** | **丢弃**未发送草稿；列表切换为新小节**全量**记录 |
| P3-07-04 | 在 P3；发送请求成功返回 | 无操作 | 聊天记录**更新**，出现新用户消息气泡，内容与发送框一致；输入框清空 |
| P3-07-05 | 用户发送后 NPC 轮次生成中 | 观看 | 出现 **typing** 指示（或等价「对方正在输入」） |
| P3-07-06 | NPC 回复已落库 | 观看 | 新 NPC 气泡追加至记录；typing 结束 |
| P3-07-07 | 新消息到达后列表变长 | 无操作 | 列表自动滚动或出现「回到底部」提示（实现二选一，须一致） |
| P3-07-08 | 拉取本节记录失败 | 进入该节 | 展示错误态与重试入口（可选） |

##### F-P3-08 调试模式

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3-08-01 | 在 P3 | 点击「调试模式」 | 进入 **DBG** |

---

### 3.11 P3a 章节列表弹窗

#### 页面作用与布局

- **作用**：浏览本书全部章—节；**Demo 豁免**：可切换到任意节为当前练习节（§5.6.1）。  
- **布局**：标题「章节列表」；可滚动章—节树；每节选中态展示摘要/任务/人物；底栏「关闭」「确认进入」。

#### 功能清单

##### F-P3a-01 浏览与选择

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3a-01-01 | P3a 打开 | 滚动列表 | 可浏览全部小节 |
| P3a-01-02 | P3a 打开 | 点选某一节 | 该节为选中态；展示对应摘要/任务/人物 |
| P3a-01-03 | P3a 打开 | 未点确认仅关闭 | 当前运行小节不变 |

##### F-P3a-02 确认进入（G9）

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3a-02-01 | 已选中某节 | 点击「确认进入」 | 关闭弹窗；P3 切换为该节上下文；对话列表加载该节数据 |
| P3a-02-02 | 未选中任何节 | 点击「确认进入」 | 阻止或 Toast |
| P3a-02-03 | 选中节与当前节相同 | 点击「确认进入」 | 关闭弹窗；可无网络请求或轻量刷新 |

##### F-P3a-03 关闭

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| P3a-03-01 | P3a 打开 | 点击「关闭」或遮罩 | 关闭弹窗；不改变当前节 |

---

### 3.12 DBG 调试独立页

#### 页面作用与布局

- **作用**：承载与 C 端解耦的调试能力（复用现有调试实现或占位）。  
- **布局**：由工程现状决定；须提供离开调试返回产品的路径。

#### 功能清单

##### F-DBG-01 进入与离开

| 故事编号 | 前置条件 | 用户操作 | 预期结果 |
|----------|----------|----------|----------|
| DBG-01-01 | 任意 C 端页 | 点击「调试模式」 | 路由至调试页 |
| DBG-01-02 | 在调试页 | 点击返回/关闭 | 回到合理上游（P1/P2/P3 或浏览器后退，由实现约定） |

---

## 4. 验收清单（汇总）

- [ ] 每次冷启动进入 **P1**；P1 → P2 正常。  
- [ ] P2 列表与包一一对应；删除链 P2a→P2b 正确。  
- [ ] 创作向导 **Tab 不可跳步**；P2.1→P2.5 顺序与取消/返回行为符合 **G3、G4**。  
- [ ] **P2.2、P2.4** 具备 **G10** 当前进度文案（含接口驱动更新与无阶段时的兜底），等待过程可感知分步推进。  
- [ ] P2.5 无返回 P2.1；「完成」回 P2，「探索」进 P3 且默认 `S[0]`。  
- [ ] P3 四主能力弹窗行为符合 **G6、G8**（含单/多 NPC 默认与浮层）；**查看列表** 打开 P3a。  
- [ ] P3 聊天记录展示**当前小节全量**回合；发送成功后列表更新；**返回首页**丢弃未发送草稿。  
- [ ] P3a **任意节可进**（Demo）；关闭不改节，确认后改节。  
- [ ] 「调试模式」自各页可达 **DBG** 且与 C 端壳层分离。

---

## 5. 更新记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1.6 | 2026-05-09 | 移除已删除的 **`ui-flow-wireframes.md`** 引用 |
| v0.1.5 | 2026-05-09 | 增加与 **`ui-flow-wireframes.md`** 的交叉引用（线框对照 + `assets/ui-flow-wireframes.png` 占位） |
| v0.1.4 | 2026-05-09 | 适用范围增加 **`technical-implementation-skeleton.md`** 工程落地引用 |
| v0.1.3 | 2026-05-09 | 与场景包 PRD 一并迁入 **`docs/product/scenario-package-standalone/`**；适用范围内互链改为同目录文件名 |
| v0.1.2 | 2026-05-09 | **P3**：返回首页**丢弃**输入草稿（P3-01-02）；**G8** 细化单/多 NPC、浮层选人、默认第一项；**F-P3-07** 本节全量聊天记录与发送后刷新、切节丢草稿（P3-07-03） |
| v0.1.1 | 2026-05-09 | **G10**：P2.2 / P2.4 增加「当前进度文案」分步反馈；§1 总览、§3.6 / §3.8 功能与用户故事、§4 验收对齐 |
| v0.1.0 | 2026-05-09 | 初版：页面总览 + 分页面功能与用户故事；对齐场景包 PRD §5.6.1 Demo 豁免与 R1/R2 行为 |
